# Example Configuration for Prompt Agent - Phase 6

# LLM Engine Configuration
engines:
  # OpenAI Configuration
  # openai:
  #   api_key: env:OPENAI_API_KEY      # Set via environment variable
  #   model: gpt-4-mini                 # Model name
  #   timeout: 120s                      # Request timeout

  # Google Gemini Configuration
  gemini:
    api_key: env:GEMINI_API_KEY       # Set via environment variable
    model: gemini-2.5-flash-lite           # Model name
    timeout: 120s                      # Request timeout

# UI Configuration
ui:
  output: tui                          # Options: "stdout" or "tui"
  # output: stdout                     # Use stdout for plain text output

# # Trigger Configuration
# triggers:
#   enabled: false
#   default_prompt: "explain_code"
#   hotkeys:
#     shift+cmd+a: "explain_code"
#     shift+cmd+r: "code_review"
#   mouse: {}

# # Selection Configuration
# selection:
#   provider: auto                       # auto, macos, linux, windows
#   fail_on_empty_selection: true
#   trim_whitespace: true
